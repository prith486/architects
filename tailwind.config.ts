import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      colors: {
        gold: {
          primary: '#C9A96E',
          muted: '#B8914A',
          light: '#DFC28A',
          shimmer: '#E8D5A3',
        },
      },
    },
  },
};

export default config;
